@app.post("/maintenance")
def maintenance(vehicle_id: str, user_id: str, mileage: float, months: int, signals: list):

    return maintenance_service.process(
        vehicle_id,
        user_id,
        mileage,
        months,
        signals
    )
