@app.post("/inspection")
def run_inspection(vehicle_id: str, user_id: str, image_path: str):

    result = inspection_service.process(
        vehicle_id,
        user_id,
        image_path
    )

    return result
