# Core Framework
Flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0

# Redis for rate limiting (production)
redis==5.0.1

# Environment & Configuration
python-dotenv==1.0.0

# HTTP Client (for M-Pesa & external APIs)
requests==2.31.0

# Supabase (Database & Auth)
supabase>=2.0.0,<3.0.0

# Production Server
gunicorn==21.2.0

# PDF Generation (for valuation/inspection reports)
reportlab==4.0.4

# Add FastAPI (temporary fix for mysterious import)
fastapi==0.104.1
uvicorn==0.24.0
